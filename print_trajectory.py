import pickle
import sys

if __name__ == "__main__":
    # fname = sys.argv[1]
    # with open(fname, "rb") as f:
    #     d = pickle.load(f)
    # print(type(d["pose"]))
    # item = d["pose"][1]
    # print(item.__dict__)

    # item.from_frame = "franka_tool"
    # item.to_frame = "world"
    # with open("test.pkl", "wb") as f:
    #     pickle.dump(item, f)
    # print(type(item.quaternion))
    # print(item.quaternion)
    # print(item.quaternion.dtype)

    # import cv2
    # import img_lib
    # img = cv2.imread("new_base.png")
    # with open("strokes/data.pkl", "rb") as f:
    #     d = pickle.load(f)
    #     import matplotlib.pyplot as plt
    # img = img_lib.prep_img(img)
    # plt.imshow(img)
    # plt.show()
    # print(img.shape)
    # d["base"] = img
    # with open("strokes/data.pkl", "wb") as f:
    #     pickle.dump(d, f)

    # print(item.translation)